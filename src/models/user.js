class User {
  constructor(email, password, name) {
    this.email = email;
    this.password = password;
    this.name = name;
  }
}

export default User;


/* 
  new User(email, password, name)
  when we register a new user this needs to be done

*/